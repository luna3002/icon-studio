# Cloudflare Workers 로 백엔드를 배포할 때만 사용.
# (Vercel 을 쓰면 이 파일은 무시해도 됩니다.)
name = "icon-studio-gemini"
main = "cloudflare-worker.js"
compatibility_date = "2024-09-23"

# 키는 secret 으로 등록 (파일에 적지 말 것):
#   npx wrangler secret put GEMINI_API_KEY
# 모델을 바꾸려면 아래 주석을 해제:
# [vars]
# GEMINI_MODEL = "gemini-2.5-flash"
